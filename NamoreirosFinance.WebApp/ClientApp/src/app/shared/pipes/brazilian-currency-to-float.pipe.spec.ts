import { BrazilianCurrencyToFloatPipe } from './brazilian-currency-to-float.pipe';

describe('BrazilianCurrencyToFloatPipe', () => {
  it('create an instance', () => {
    const pipe = new BrazilianCurrencyToFloatPipe();
    expect(pipe).toBeTruthy();
  });
});
